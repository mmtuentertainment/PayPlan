/**
 * Animation Constants
 * Feature: 017-navigation-system
 *
 * Centralized animation timing and duration values to ensure consistency
 * across components and Tailwind configuration.
 */

/**
 * Mobile menu slide-in/out animation duration in milliseconds
 * Used in:
 * - MobileMenu.tsx (transition-duration CSS class)
 * - tailwind.config.ts (fade-in animation definition)
 */
export const MOBILE_MENU_ANIMATION_DURATION = 300;

/**
 * Fade-in animation duration in milliseconds
 * Used for backdrop overlay animations
 */
export const FADE_IN_DURATION = 300;
